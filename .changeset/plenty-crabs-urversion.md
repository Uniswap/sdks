---
'@uniswap/v4-sdk': minor
---

Add URVersion.V2_3_0 so universal-router-sdk's toV4URVersion resolves the Universal Router v2.3.0 release (the version whose UNWRAP_WETH command takes an explicit exact amount).
